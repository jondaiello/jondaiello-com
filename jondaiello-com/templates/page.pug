
extends layout

block append vars
  - bodyclass = 'page-single'

block prepend title
  | #{ page.title + ' - '}

block header
  h1= page.title

block content
  article.article
    section.content!= typogr(page.html).typogrify()

